//base
import { FC } from "react";

//components
import { CartContainer } from "@/components/containers/Cart/CartContainer/CartContainer";

const cart: FC = (): JSX.Element => {
  return <CartContainer />;
};

export default cart;
